#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Caractere de escape \n salta uma linha.

    // Função de saída
    // O printf() retorna um número
    printf("\nValor retornado pelo printf: (quantidade de caracteres) %d", printf("Ola!"));

    // %d é uma máscara para números inteiros
    // system("pause") só funciona no Windows.

    return 0;
}
